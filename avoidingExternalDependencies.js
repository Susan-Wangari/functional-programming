//an advantage to avoiding external dependencies is that you can easily debug your code
// The global variable
var fixedValue = 4;
//a function that takes an argument and returns the iuncreased value
function incrementer (changingValue) {
return changingValue +1;

}
